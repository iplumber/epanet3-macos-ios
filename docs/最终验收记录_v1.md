# EPANET macOS/iOS 最终验收记录 v1

## 1. 基本信息
- 验收版本：v1
- 验收日期：2026-03-18
- 验收范围：A/B/C/D/E 主线阶段
- 验收依据：`docs/验收标准_v1.md`

## 2. 结论
- 结论：通过
- 结论说明：v1 主流程闭环可用，关键算例回归通过，达到发布收尾条件。

## 3. 验收项结果

### 3.1 文件管理
- [x] 可打开 `.inp`
- [x] 可保存与另存 `.inp`（最小集）
- [x] 打开失败有明确错误提示
- 验收结果：通过

### 3.2 对象操作
- [x] 可选中对象并查看属性
- [x] 可编辑节点/管段核心属性
- [x] 可删除对象（选中删除 + 按 ID 删除）
- 验收结果：通过

### 3.3 参数设置
- [x] 可设置主要计算参数（Trials/Accuracy/DemandMult/Duration/HydStep/ReportStep）
- [x] 可进行 Flow Units `GPM/LPS` 切换（重载式）
- 验收结果：通过（最小集）

### 3.4 计算与反馈
- [x] 可运行计算
- [x] 成功/失败反馈可用
- [x] 错误信息可读且具备上下文
- 验收结果：通过

### 3.5 结果显示
- [x] 支持压力/流量结果上图
- [x] 图例显示可用
- [x] 对象结果读取链路可用
- 验收结果：通过（最小集）

## 4. 回归结果
- [x] `net1.inp`：通过
- [x] `any1.inp`：通过
- [x] `400.inp`：通过
- [x] `10000.inp`：通过（性能/稳定性）
- [x] `90000.inp`：通过（性能/稳定性）
- [x] CLI `--self-check-errors`：通过

## 5. 风险与遗留
- Flow Units 切换当前为临时副本重载策略，后续可评估原位切换能力。
- 结果上图指标仍属最小集，建议在 v1.x 增加更多指标与时间步浏览。
- iOS 小屏界面与手势效率建议在下一阶段集中优化。

## 6. 后续动作
- [ ] 创建 `v1` tag 或发布分支
- [ ] 完成对外发布文案（可复用 `docs/发布说明_v1.md`）
- [ ] 启动 P1：macOS 界面强化（浅色/深色一致性与交互打磨）
